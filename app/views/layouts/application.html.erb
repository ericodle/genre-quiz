<!DOCTYPE html>
<html>
  <head>
    <title>Music Genre Quiz</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <div class="container">
      <header>
        <h1>Music Genre Classification Quiz<br><span style="font-size: 0.6em; font-weight: 400;">音樂類型分類測驗</span></h1>
        <p class="subtitle">Test your ability to identify musical genres<br>測試您識別音樂類型的能力</p>
      </header>

      <% if flash[:alert] %>
        <div class="alert alert-error">
          <%= flash[:alert] %>
        </div>
      <% end %>

      <% if flash[:notice] %>
        <div class="alert alert-success">
          <%= flash[:notice] %>
        </div>
      <% end %>

      <main>
        <%= yield %>
      </main>

      <footer>
        <p>Music Genre Quiz - Research Project<br>音樂類型測驗 - 研究專案</p>
      </footer>
    </div>
  </body>
</html>

